# Setup

- Create venv
  `python -m venv <name>`

- Activate venv
  `source <name>/bin/activate`

- Install packages
  `pip install -r requirements.txt`

<br>

# Dates.json

- Populate dates.json

**Dates will auto update every 5 minutes**

<br>

# Run

`python main.py`
